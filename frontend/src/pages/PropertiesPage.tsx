import { Building2 } from 'lucide-react';
import PagePlaceholder from '../components/PagePlaceholder';

export default function PropertiesPage() {
  return (
    <PagePlaceholder
      title="Properties"
      description="The properties list, create form, and detail view land in the next session."
      icon={Building2}
    />
  );
}
