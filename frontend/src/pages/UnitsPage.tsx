import { DoorOpen } from 'lucide-react';
import PagePlaceholder from '../components/PagePlaceholder';

export default function UnitsPage() {
  return (
    <PagePlaceholder
      title="Units"
      description="Unit management UI ships with the entity CRUD pass."
      icon={DoorOpen}
    />
  );
}
