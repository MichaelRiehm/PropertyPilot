import { FileText } from 'lucide-react';
import PagePlaceholder from '../components/PagePlaceholder';

export default function LeasesPage() {
  return (
    <PagePlaceholder
      title="Leases"
      description="Lease management UI ships with the entity CRUD pass."
      icon={FileText}
    />
  );
}
