{
  "name": "propertypilot",
  "version": "0.1.0",
  "private": true,
  "description": "PropertyPilot — property management web app for small residential landlords (WGU D424 capstone).",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm:dev -w backend\" \"npm:dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "test": "npm run test -w backend && npm run test -w frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
